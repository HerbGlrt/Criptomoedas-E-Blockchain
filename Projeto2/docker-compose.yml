version: '3.8'

services:
  web:
    image: nginx:latest
    container_name: donation-web
    ports:
      - "8080:80"
    volumes:
      - ./web:/usr/share/nginx/html:ro
    networks:
      - donation-network

networks:
  donation-network:
    driver: bridge
